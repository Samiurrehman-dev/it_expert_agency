import {getAuthenticatedUser}from"@/lib/apiAuth";import{redirect}from"next/navigation";import{ContentForm}from"@/components/dashboard/content-form";export default async function Page({searchParams}:{searchParams:{slug?:string}}){const u=await getAuthenticatedUser();if(!u)redirect("/login");if(!searchParams.slug)redirect("/dashboard/blogs");return <ContentForm kind="blogs" user={u} slug={searchParams.slug}/>}
